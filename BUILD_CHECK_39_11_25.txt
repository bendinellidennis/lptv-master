MALTA DRIVING MASTER — BUILD 39.11.25
BASE: 39.11.24 INLINE FREEZE IMAGES
FIX: remove black empty area below static freeze images.
CAUSE: malformed literal \\n sequences in CSS tail could invalidate the local-freeze rules in Koder; image then rendered at natural aspect ratio instead of filling the 620/560px Replay stage.
FIXES:
- cleaned malformed CSS newlines;
- added final high-specificity full-stage rules;
- added inline critical dimensions directly to replay-local-freeze wrapper and img;
- 01/02/03 image: absolute inset 0, width/height 100%, object-fit cover;
- phase 04 video unchanged.
No database, Replay Engine, scenes, Coach, answers, mapping or media content changed.
