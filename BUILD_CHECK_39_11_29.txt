MALTA DRIVING MASTER — BUILD CHECK 39.11.29

Purpose:
Fix GitHub-visible delayed "Loading video" in special Replay templates CARS2.6 and CARS2.4.

Root cause found:
- CARS2.6 pedestrianWaveAcross and CARS2.4 zebraWaitingStop still used ReplayEngine.renderVideoMarkup() in phases 01-03.
- On local/Koder the video could appear quickly, masking the legacy behavior.
- On GitHub Pages the network delay exposed the "Loading video" state.

Fix:
- CARS2.6 phases 01-03 now render the embedded photographic freeze directly from window.REPLAY_FREEZE_INLINE.pedestrianWaveAcross.
- CARS2.4 phases 01-03 now render the embedded photographic freeze directly from window.REPLAY_FREEZE_INLINE.zebraWaitingStop.
- Phase 04 remains the same approved video for both.
- No question, correct answer, Coach text, mapping, hotspot, or approved video changed.

Checks:
PASS — node --check app-3994.js
PASS — embedded freeze key pedestrianWaveAcross present
PASS — embedded freeze key zebraWaitingStop present
PASS — special template phase<3 uses embedded freeze
PASS — special template phase 04 uses original ReplayEngine video
PASS — standard Replay template unchanged
